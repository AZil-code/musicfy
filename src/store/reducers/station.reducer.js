import { stationService } from '../../services/station.service';

const initialState = {};

export function stationReducer(state = initialState, cmd) {
   // switch (cmd.type) {
   //     case
   // }
}

// When do we need global state for stations?
